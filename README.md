## Setup

1. Clone the repository:
    ```bash
    git clone https://github.com/Phe-nix/unit-test.git
    cd unit-test
    ```

2. Install dependencies and start service:
    ```bash
    npm install
    npm start
    ```

3. start testing:
    ```bash
    npm test
    ```
